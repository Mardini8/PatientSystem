apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: patientsystem
spec:
  selector:
    app: user-service
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
    nodePort: 30081
  type: NodePort